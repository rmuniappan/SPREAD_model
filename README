The code is organized as follows:

data/ contains some data not found in the database data_and_obj.zip.

processed/ contains intermediate files generated by one module and used by
another.

multi_pathway_model/ contains the implementation of the model, evaluation
and analysis scripts.

long_distance/ contains the implementation of commodity flow networks.

clustering/ contains the clustering analysis scripts.

Contact: abhijin@virginia.edu
